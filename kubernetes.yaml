apiVersion: apps/v1
kind: Deployment
metadata:
  name: hillastarter
  namespace: default
spec:
  replicas: 2
  selector:
    matchLabels:
      app: hillastarter
  template:
    metadata:
      labels:
        app: hillastarter
    spec:
      containers:
        - name: hillastarter
          image: hillastarter:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8080
          resources:
            limits:
              memory: 512Mi
              cpu: '1'

---
apiVersion: v1
kind: Service
metadata:
  name: hillastarter-balancer
spec:
  selector:
    app: hillastarter
  ports:
    - protocol: TCP
      port: 8000 # The port to expose to the outside
      targetPort: 8080 # The port the application is running on in the pods
  type: LoadBalancer
  sessionAffinity: ClientIP
